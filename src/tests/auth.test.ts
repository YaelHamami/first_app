import { request } from "http"

describe("Restric access without Auth / ", () => {
    test("getAllPosts. should respond with error", async () => {
        // const res = await request('').get("/post")
    })
})